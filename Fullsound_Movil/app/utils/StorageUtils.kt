package com.fullsound.app.utils

import android.content.Context
import com.fullsound.app.models.Beat

object StorageUtils {
    fun getBeats(context: Context): List<Beat> {
        // Implementar lectura de beats desde almacenamiento local
        return listOf()
    }
    fun saveBeat(context: Context, beat: Beat) {
        // Implementar guardado de beat
    }
    // Métodos para carrito, admin, etc.
}
